{% extends 'back/layout.html.twig' %}

{% block title %} Voir {{'Configuration'|transchoice(1)}} {% endblock %}

{% block content %}

<section class="section-padding">
    <div class="container">
        <h1>Voir {{'Configuration'|transchoice(1)}}</h1>
        <div class="shape"></div>

        <div class="text-right mt-4">
            <a href="{{ path('admin_configuration_search') }}" class="btn btn-primary" role="button">
                <i class="fa fa-reply"></i> Retour
            </a>
        </div>
        <div class="card mt-4 mb-4">
            <div class="card-header text-center">
                <h4>Détail {{'Configuration'|transchoice(1)}}</h4>
            </div>
            <div class="card-content">
                    <div class="read-content">
                        <p>
                            <strong>{% trans %}Name{% endtrans %}</strong> : 
                            {{ configuration.name }}
                        </p>
                    </div>
                                    <div class="read-content">
                        <p>
                            <strong>{% trans %}Value{% endtrans %}</strong> : 
                            {{ configuration.value }}
                        </p>
                    </div>
                            </div>
        </div>
    </div>
</section>
{% endblock %}